<!--
  À FUSIONNER dans android/app/src/main/AndroidManifest.xml (généré par
  `npx react-native init`), dans <manifest> et <application>.
-->

<!-- Permissions, en dehors de <application> -->
<uses-permission android:name="android.permission.RECORD_AUDIO" />
<uses-permission android:name="android.permission.READ_PHONE_STATE" />
<uses-permission android:name="android.permission.READ_CALL_LOG" />
<uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
<uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

<!-- À l'intérieur de <application> -->

<!-- Service optionnel : détection d'appel (n'accède pas au flux audio) -->
<service
    android:name=".VoicemailCallScreeningService"
    android:permission="android.permission.BIND_SCREENING_SERVICE"
    android:exported="true">
    <intent-filter>
        <action android:name="android.telecom.CallScreeningService" />
    </intent-filter>
</service>

<!-- Service d'accessibilité : contournement du blocage VOICE_CALL -->
<!-- L'utilisateur doit l'activer manuellement : Réglages > Accessibilité -->
<service
    android:name=".CallAutoSpeakerService"
    android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
    android:exported="false">
    <intent-filter>
        <action android:name="android.accessibilityservice.AccessibilityService" />
    </intent-filter>
    <meta-data
        android:name="android.accessibilityservice"
        android:resource="@xml/accessibility_service_config" />
</service>
